# Core MCP dependencies
mcp>=1.0.0

# Web scraping and HTTP
crawl4ai>=0.7.3
httpx>=0.27.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database and caching
aiosqlite>=0.19.0

# Embeddings (optional - for better search)
numpy>=1.24.0
tiktoken>=0.6.0

# Docling for advanced chunking and document processing
docling>=2.0.0
docling-core>=2.0.0
transformers>=4.30.0
torch>=2.0.0  # Required by transformers for tokenizer

# Configuration
python-dotenv>=1.0.0

# Ollama integration
ollama>=0.6.0
aiohttp>=3.9.0  # Required for OllamaClient async HTTP

# Version resolution
packaging>=23.0  # For version parsing and comparison

# Search alternatives (choose one or both)
duckduckgo-search>=6.0.0  # Free web search, no API key

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
black>=24.0.0
